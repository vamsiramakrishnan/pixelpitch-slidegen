# Slidify

Convert HTML slides into editable PowerPoint. This local package is included
with Pixelpitch Slidegen so renderer builds do not need the parent repository.

Run `slidify guide` for conversion and authoring documentation. See
`../../renderer/README.md` for the HTTP service and its fidelity checks.
