# Submit from agents/pixelpitch-slidegen, never the repository root.
steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - mcp-app/Dockerfile
      - -t
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
timeout: 1800s
